<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidcoding.codinf">

    <application
        android:allowBackup="true"
        android:icon="@drawable/codinf_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/codinf_logo"
        android:supportsRtl="true"
        android:theme="@style/MainTheme">
        <activity android:name=".IssueActivity"></activity>
        <activity android:name=".Issues" />
        <activity android:name=".Repositories" />
        <activity android:name=".UserActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>